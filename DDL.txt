CREATE TABLE Employees (
    ID Integer AUTO_INCREMENT PRIMARY KEY,
    Name VarChar(50),
    Email VarChar(50),
    Mobile VarChar(50),
    HireDate Date
);

CREATE TABLE Users (
    ID Integer AUTO_INCREMENT PRIMARY KEY,
    Name VarChar(50),
    Email VarChar(50) UNIQUE,
    Passwd VarChar(50)
);

CREATE TABLE StatusTypes (
    ID Integer AUTO_INCREMENT PRIMARY KEY,
    Type VarChar(50) UNIQUE
);

CREATE TABLE Attendance (
    Day Date,
    EmployeeID Integer,
    StatusTypeID Integer,
    WorkingHours Integer,
    
    PRIMARY KEY (EmployeeID, Day),
    FOREIGN KEY (EmployeeID) REFERENCES Employees (ID),
    FOREIGN KEY (StatusTypeID) REFERENCES StatusTypes (ID)
);

INSERT INTO statustypes (Type) VALUES ('Present'), ('Absent'), ('Sick Leave'), ('Day OFF');

INSERT INTO users (Name, Email, Passwd) VALUES ('Ahmed Hussein', 'ahkcsit@gmail.com', '123');